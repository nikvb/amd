; http.conf for the app_amd_ws test harness: the built-in HTTP server stays
; off.  res_http_websocket only needs the core HTTP code to be present, not
; listening; the module under test uses its *client* side.
[general]
enabled = no
